package bisection;

public class Bisection {

  /** Beräkna nollstället för funktionen f i intervallet low..high med precisionen eps.   
      Förutsätter att det finns ett nollställe i intervallet. */
	public static double getZero(double low, double high, double eps, Function f) {
		if(f.evaluate(x)<=0+eps || f.evaluate(x)>=0-eps)
	} 	

}
